num = int(input("Введите число:")) #Ввод числа

#Начинаем считать с 1
current_num = 1
#Цикл будет повторяться пока не привысит num
while current_num <= num:
    if not (5 < current_num < 9) or (17 < current_num < 37) or (78 < current_num < 87):
        # Если введенное число НЕ попадает в эти искл. диапозоны то мы его выводим
        print(current_num)
#Увеличиваем счетчик для перехода к след. числу
current_num += 1
